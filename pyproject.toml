[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
    "--snapshot-update",
]
testpaths = ["tests"]
pythonpath = ["python/lambda", "python"]
[tool.pyright]
exclude = ["**/cdk.out", "**/node_modules", "**/venv", "**/__pycache__", "**/.*"]